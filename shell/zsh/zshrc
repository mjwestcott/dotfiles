# shellcheck shell=bash
#
# Executes commands at the start of an interactive session.
#

# Source profile.
[[ -e ~/dotfiles/shell/profile ]] && source ~/dotfiles/shell/profile

# Source Antidote (replaces Prezto).
source ~/.antidote/antidote.zsh
antidote load "${ZDOTDIR:-$HOME}"/.zsh_plugins.txt

# Configure zsh-autosuggestions for better performance
export ZSH_AUTOSUGGEST_USE_ASYNC=1

# Configure zsh-history-substring-search key bindings
bindkey '^[[A' history-substring-search-up
bindkey '^[[B' history-substring-search-down
bindkey '^[OA' history-substring-search-up
bindkey '^[OB' history-substring-search-down

setopt HIST_IGNORE_SPACE
export HISTSIZE=100000
export SAVEHIST=100000
mkdir -p "$XDG_STATE_HOME/zsh"
export HISTFILE="$XDG_STATE_HOME/zsh/history"
[[ -z "$TMPDIR" ]] && TMPDIR=/tmp

# Set up Homebrew on both Apple Silicon and Intel Macs. Homebrew is not on the
# default PATH immediately after a clean install, so probe its standard paths.
if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
elif [[ -x /usr/local/bin/brew ]]; then
  eval "$(/usr/local/bin/brew shellenv)"
fi

# Initialize zoxide (smarter cd)
eval "$(zoxide init zsh)"

# Source 'fzf'.
[[ -f ~/.fzf.zsh ]] && source ~/.fzf.zsh

# GCP is installed separately from this repo, so only load it when present.
if command -v brew > /dev/null 2>&1; then
  gcloud_zsh_dir="$(brew --prefix)/share/google-cloud-sdk"
  [[ -r "$gcloud_zsh_dir/path.zsh.inc" ]] && source "$gcloud_zsh_dir/path.zsh.inc"
  [[ -r "$gcloud_zsh_dir/completion.zsh.inc" ]] && source "$gcloud_zsh_dir/completion.zsh.inc"
  unset gcloud_zsh_dir
fi

# Since it's provided by our prompt.
export VIRTUAL_ENV_DISABLE_PROMPT=1

# Initalize rbenv.
command -v rbenv > /dev/null 2>&1 && eval "$(rbenv init -)"

# Initialize Starship prompt (replaces Spaceship)
eval "$(starship init zsh)"

[[ -r ~/.config/broot/launcher/bash/br ]] && source ~/.config/broot/launcher/bash/br
